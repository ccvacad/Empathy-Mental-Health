# Requiere Python 3.11 y Apple Silicon compatible
numpy==1.26.4
pandas>=1.5.0
scikit-learn>=1.2.0
torch==2.1.0; platform_machine == "arm64"  # Versión compatible con Python 3.11 y Apple Silicon
torchvision==0.16.0; platform_machine == "arm64"
tqdm>=4.65.0
transformers==4.40.0  # Versión moderna compatible con torch 2.x y Python 3.11
